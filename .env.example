# ─── Database ──────────────────────────────────────────────────────────────
# Local:
DATABASE_URL="postgresql://username:password@localhost:5432/referees_hub?schema=public"
# Vercel (Neon / Supabase / Railway):
# DATABASE_URL="postgresql://user:pass@host/dbname?sslmode=require"

# ─── NextAuth ───────────────────────────────────────────────────────────────
# Local:
NEXTAUTH_URL="http://localhost:3000"
# Production on Vercel — set to your actual domain:
# NEXTAUTH_URL="https://referees-h-ub.vercel.app"
NEXTAUTH_SECRET="your-secret-key-here-generate-with-openssl-rand-base64-32"
# Generate: openssl rand -base64 32

# ─── Admin Setup ────────────────────────────────────────────────────────────
# Used by the one-time setup endpoint POST /api/setup
# Change this before deploying!
SETUP_SECRET="setup-referees-hub"

# Optional admin credentials for the seed script (npm run db:seed)
ADMIN_EMAIL="admin@refereeshub.com"
ADMIN_PASSWORD="Admin@2025!"
ADMIN_NAME="Admin"
